<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Login</title>
        <link rel="stylesheet" href="{{ Blog.Path }}/styles/messages.css">
        <link rel="stylesheet" href="{{ Blog.Path }}/styles/login.css">
        <link rel="stylesheet" href="{{ Blog.Path }}/styles/scrollbar.css">
    </head>
    <body>
        <div class="messages">
            {{#each Response.Errors as |Error|}}
                <div class="alert">
                    <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
                    {{Error}}
                </div>
            {{/each}}
        </div>
        <div class="login">
            <div class="login-box">
                <label>Username: </label>
                <input type="text" class="username" id="username" placeholder="Username">
                <label>Password: </label>
                <input type="password" class="password" id="password" placeholder="Password">
                {{#if Setup}}
                    <label>Validate Password: </label>
                    <input type="password" class="password" id="validate-password" placeholder="Validate password">
                {{/if}}
                <label></label>
                {{#if Setup}}
                    <button id="login" onclick="RegisterUser()">Register</button>
                    {{else}}
                        <button id="login" onclick="Login()">Login</button>
                {{/if}}
            </div>
        </div>
    </body>
    <footer>
        <script type="text/javascript" src="{{ Blog.Path }}/js/sha512.js"></script>
        <script type="text/javascript" src="{{ Blog.Path }}/js/login.js"></script>
    </footer>
</html> 