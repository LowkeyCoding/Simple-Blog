<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ Blog.Path }}/styles/post.css">
    <link rel="stylesheet" href="{{ Blog.Path }}/styles/markdown.css">
    <link rel="stylesheet" href="{{ Blog.Path }}/styles/code.css">
    <link rel="stylesheet" href="{{ Blog.Path }}/styles/code.css">
    <script type="text/javascript" src="{{ Blog.Path }}/js/post.js"></script>
    <title>{{ Post.Title }}</title>
</head>
<body>
    <div class="container">
        <div class="blog_post">
            <div class="title">{{ Post.Title }}</div>
            <div class="content">
                {{#markdown Post.HTML}}{{/markdown}}
            </div>
        </div>
        {{#if LoggedIn}}
            <div class="admin_tools">
                <div class="middle">
                    <a href="{{Blog.Path}}/editor/{{Post.ID}}">Edit Post</a>
                    <div class="seperator">&nbsp|&nbsp</div>
                    <div class="button" onclick="deletePost({{Post.ID}})">Delete Post</div>
                </div>
            </div>
        {{/if}}
    </div>
</body>
</html>